# Core dependencies for Prompt Circle skills scripts
# These are the runtime dependencies needed by the Python scripts

# HTTP client (replaces urllib.request for better error handling)
requests>=2.31.0

# Type checking and validation
pydantic>=2.0.0

# Linting and formatting
ruff>=0.4.0
mypy>=1.10.0

# Testing
pytest>=8.0.0
pytest-cov>=4.1.0

# Security scanning
bandit>=1.7.0
safety>=2.3.0

# Development dependencies (optional)
# pip install -e ".[dev]"
